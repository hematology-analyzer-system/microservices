spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/testorder
    username: postgres
    password: trquth1209
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update         # update | validate | create | create-drop
    show-sql: true             # Hi?n th? c�u SQL trong console
    properties:
      hibernate:
        format_sql: true       # Format SQL cho d? ??c

server:
  port: 8082    # C� th? ??i port n?u c?n
  servlet:
    context-path: /testorder

jwt:
  secret: "my-super-ultra-promax-vip-pro-password-32-bytes-long"